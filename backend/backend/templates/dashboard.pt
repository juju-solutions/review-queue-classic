<!DOCTYPE html>
<html>
<head>
  <!-- Standard Meta -->
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
  <title>Review Queue</title>
  <link href='http://fonts.googleapis.com/css?family=Ubuntu' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" type="text/css" href="${request.static_url('backend:static/css/semantic.min.css')}">
  <link rel="stylesheet" type="text/css" href="${request.static_url('backend:static/css/main.css')}">
</head>
<body id="home">
  <div class="ui page grid">
    <div class="sixteen wide column">
      <h1>Incoming Charms  (${len(incoming)})</h1>
      <div>
        <p>A list of all new incoming charms! Shortly a reviewer will be by to
        perform an initial review of your charm. To make this process smoother make
        sure you've read all <a href=#>Charm Store policies</a> and have <code>charm proof</code>
        passing without an ERROR or WARNING messages.</p>
      </div>
      <table class="ui table segment sortable">
        <thead>
          <tr>
            <th class="descending">Age</th>
            <th class="eleven wide">Title</th>
            <th class="two">Owner</th>
            <th><i class="chat icon"></i></th>
          </tr>
        </thead>
        <tbody>
          <tr tal:repeat="review incoming">
            <td>${review.age}</td>
            <td><a href="${review.url}">${review.title}</a></td>
            <td>${review.owner.name}</td>
            <td>${len(review.votes)}</td>
          </tr>
        </tbody>
      </table>
      <h1>Charm Reviews (${len(reviews)})</h1>
      <div>
        <p>This contains all reviews which require attention from a chamer to complete.
        If you're an author and see your item in this queue, congratulations! Reviews
        are processed in a top down fashion and yours should be reviewed soon. If you
        do not see your review in this queue after 20 minutes please check <a href="#">/user/~lpuser</a>
        to find next actions for your review.</p>
        <p>Those wishing to review items please refer to the
          <a href="#">Charm Review Process</a> before continuing.
        </p>
      </div>
      <table class="ui table segment sortable">
        <thead>
          <tr>
            <th class="descending">Age</th>
            <th>State</th>
            <th class="eleven wide">Name</th>
            <th><i class="chat icon"></i></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <tr tal:repeat="review reviews">
            <td data-sort="${review.age.split(' ')[0]}">${review.age}</td>
            <td>${review.state}</td>
            <td><a href="${review.url}">${review.title}</a></td>
            <td>
              ${len(review.votes)}
            </td>
            <td>
            </td>
          </tr>
        </tbody>
      </table>
      <h1>Documentation Reviews</h1>
      <table class="ui table segment sortable">
        <thead>
          <tr>
            <th></th>
            <th class="descending">Age</th>
            <th>State</th>
            <th>Type</th>
            <th class="eight wide">Name</th>
            <th>Spread</th>
          </tr>
        </thead>
        <tbody>
        </tbody>
      </table>
      <h1>Tools Reviews</h1>
      <table class="ui table segment sortable">
        <thead>
          <tr>
            <th></th>
            <th class="descending">Age</th>
            <th>State</th>
            <th>Type</th>
            <th class="eight wide">Name</th>
            <th>Spread</th>
          </tr>
        </thead>
        <tbody>
        </tbody>
      </table>
      <h1>Ask Ubuntu</h1>
    </div>
  </div>
  <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/2.0.3/jquery.js"></script>
  <script src="${request.static_url('backend:static/javascript/semantic.min.js')}"></script>
  <script src="${request.static_url('backend:static/javascript/jquery.tablesort.min.js')}"></script>
  <script src="${request.static_url('backend:static/javascript/main.js')}"></script>
</body>
</html>
